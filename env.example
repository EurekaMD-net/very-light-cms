# Very Light CMS — Environment Variables
# Copy to .env and rename to .env. Never commit .env.

# Server
PORT=3333

# Database (SQLite)
DB_PATH=./data/cms.db

# Content directory (absolute or relative to project root)
CONTENT_DIR=./content/pages
